# Stores

Global client state (e.g. Zustand stores) lives here. Reach for a store only for state that is
genuinely cross-cutting — most state should stay local to a component or feature.

No stores are implemented yet; this directory is scaffolding for future state management.
